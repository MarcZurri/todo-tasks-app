import { StateToIconPipe } from './state-to-icon.pipe';

describe('StateToIconPipe', () => {
  it('create an instance', () => {
    const pipe = new StateToIconPipe();
    expect(pipe).toBeTruthy();
  });
});
